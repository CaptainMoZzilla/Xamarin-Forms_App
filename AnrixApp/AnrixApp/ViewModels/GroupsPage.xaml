<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AnrixApp.ViewModels.GroupsPage"
             Title="All groups">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Load info" Priority="1" Order="Secondary" Clicked="ToolbarItem_Clicked" />
        <ToolbarItem Text="Mock info" Priority="2" Order="Secondary" Clicked="ToolbarItem_Clicked_1"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding .}" ItemTapped="ListView_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell ImageSource="{Binding PhotoUrl}" TextColor="Black" Text="{Binding NumberOfGroup, StringFormat='Group: {0:N}'}"  Detail="{Binding Course, StringFormat='Course: {0:D}'}">
                            <ImageCell.ContextActions>
                                <MenuItem Text="Delete"></MenuItem>
                            </ImageCell.ContextActions>
                        </ImageCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>